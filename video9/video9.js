let a = {
    name : 'Kevin',
    age: 25
};

console.log(`check data obj: ${a.name} , ${a.age}`, a);

let b = ['MU', 29, 'SUper'];

console.log(`type of b: ${typeof b}, ${b}`);
console.log('type of b: ', typeof b, b);

console.log('first item:', b[0], 'second item:', b[1]);
console.log(`first item: ${b[0]}, second item: ${b[1]}`);

let family = [
    {name: 'John', age: 59},
    {name: 'Jenn', age: 57}
];

console.log('member of the family:', family);
console.log(`member of the family:${family[0]}`);